#include <iostream>
#include <queue>
#define mfor(i,s,e,) for(auto i = s; i<= e; ++i)
#define endl '\n'
using namespace std;

int A[11][11];
int N,M,K;


int main()
{
	ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
	cin>>N>>M>>K;
	mfor(i, 1, N) {
		mfor(j, 1, N) {
			cin>>A[i][j];
			map
		}
	}

}